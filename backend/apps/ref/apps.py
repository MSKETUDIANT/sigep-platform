from django.apps import AppConfig


class RefConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.ref"
    label = "ref"
    verbose_name = "Référentiel territorial"
